//O(1) [Constant Time]. With this algorithm, it does not matter the size of the array, the number of comparisons/operations will always be constant.

const boxes = [0, 1, 2, 3, 4, 5, 6];

function firstBox (array) {
  console.log(array[0]);
}

firstBox(boxes);